# 轻量发布镜像：直接使用本地已构建好的 apps/web/dist，
# 不在服务器上跑 Vite，避免低内存机器 OOM。
# 构建产物由 scripts/deploy.sh 在本地生成。
FROM nginx:alpine
COPY apps/web/dist /usr/share/nginx/html
COPY apps/web/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
